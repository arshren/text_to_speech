name: PDf_Reader
on: [push]
jobs:
  run:
    runs-on: [ubuntu-latest]
    container: docker://python/3.8/alpine3.11
    steps:
      - uses: actions/checkout@v2
      - name: run
        env:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
        run: |

          # Your ML workflow goes here
          echo "test"
          pip install -r requirements.txt
          python Read_pdf.py
